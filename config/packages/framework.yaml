# Symfony 7.4 Framework Configuration
# CLI tool - minimal config, no HTTP features needed
framework:
    secret: '%env(APP_SECRET)%'
    php_errors:
        log: true
    handle_all_throwables: true

when@test:
    framework:
        test: true
